# Upsun configuration, generated using AI at: 2026-01-07T03:02:19+01:00
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  back:
    type: php:8.5

    build:
      flavor: none

    hooks:
      build: |
        set -ex
        composer install --no-dev --no-interaction --no-progress

    web:
      locations:
        /:
          root: back/public
          passthru: /index.php

    mounts:
      back/storage:
        source: storage

    relationships:
      database: {}

services:
  database:
    type: mariadb:11.8

routes:
  https://{default}/:
    type: upstream
    upstream: back:http