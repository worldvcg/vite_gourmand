RewriteEngine On

# Si le fichier existe → on le sert normalement
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon → tout passe vers index.php
RewriteRule ^ index.php [QSA,L]